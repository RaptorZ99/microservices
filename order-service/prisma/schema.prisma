generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Order {
  id        Int      @id @default(autoincrement())
  user      String
  item      String
  createdAt DateTime @default(now())
}
