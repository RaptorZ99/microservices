# ─────────────────────────────────────────────────────────────────
# .gitlab/ci/templates/validate.yml
#
# Stage : validate
# Templates : .job:grype (Grype, develop+main) + .job:check (Cosign verify, main)
# ─────────────────────────────────────────────────────────────────

# ── Scan de vulnérabilités via Grype ────────────────────────────
.job:grype:
  stage: validate
  image: alpine:3.19
  before_script:
    - apk add --no-cache curl
    - curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
  script:
    - echo "=== Scan de vulnerabilites pour $SERVICE_NAME ==="
    - grype sbom:sbom-$SERVICE_NAME.json
    - echo "Scan termine"
  rules:
    - if: '$CI_COMMIT_BRANCH == "develop"'
    - if: '$CI_COMMIT_BRANCH == "main"'

# ── Vérification de la signature Cosign (main uniquement) ──────
.job:check:
  stage: validate
  image: alpine:3.19
  before_script:
    - apk add --no-cache curl
    - curl -sSfL https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64 -o /usr/local/bin/cosign
    - chmod +x /usr/local/bin/cosign
    - echo "$COSIGN_PUBLIC_KEY" > /tmp/cosign.pub
  script:
    - IMAGE_GITLAB="$CI_REGISTRY_IMAGE/$SERVICE_NAME:$CI_COMMIT_SHORT_SHA"
    - echo "=== Verification de la signature de $IMAGE_GITLAB ==="
    - echo "$CI_REGISTRY_PASSWORD" | cosign login "$CI_REGISTRY" --username "$CI_REGISTRY_USER" --password-stdin
    - cosign verify --key /tmp/cosign.pub --insecure-ignore-tlog=true "$IMAGE_GITLAB"
    - echo "Signature verifiee"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
