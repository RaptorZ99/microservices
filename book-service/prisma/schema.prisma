generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model LibraryEntry {
  id        Int      @id @default(autoincrement())
  workId    String
  user      String
  createdAt DateTime @default(now())

  @@unique([user, workId])
}
